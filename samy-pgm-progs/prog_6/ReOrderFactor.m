function F_ = ReOrderFactor(F, order)
% Reorders a given factor according to the order specified for the
% variables.
  F_.var = order;
  F_.
  Idx_to_Ass = IndexToAssignment(1:prod(F.card), F.card);
  for i = 1
end

